{
  "functions": [
    {
      "runtime": "nodejs20",
      "source": "functions",
      "codebase": "default",
      "ignore": ["node_modules", ".git"],
      "predeploy": [
        "npm --prefix \"${RESOURCE_DIR}\" run build"    
      ]
    }
  ]
}
